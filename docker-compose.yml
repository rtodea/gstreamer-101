version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
  debug:
    build:
      context: .
      dockerfile: Dockerfile
    command: /bin/bash
    tty: true
    stdin_open: true
    volumes:
      - .:/app
    privileged: true
    devices:
      - /dev/snd:/dev/snd
